rootProject.name = "Bloom And Wild"

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

include ':app'
include ':domain'
include ':data'
include ':remote'
include ':view'
